---
description: scaffold a new screen with expo-router
---

create a new screen for: $ARGUMENTS

1. create the file in the correct `app/` path
2. basic layout using nativewind + View + Text
3. expo-router Stack.Screen for header config
4. wire tanstack query if screen needs data fetching

follow existing screen patterns in the codebase.
